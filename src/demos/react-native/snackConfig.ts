// Configuration for the embedded Expo Snack live demo.
//
// To activate the live demo, publish `apps/expo-demo/snack` to Expo Snack
// (https://snack.expo.dev — see apps/expo-demo/snack/README.md), then paste the
// resulting snack id/@user+slug here. While `SNACK_ID` is empty the page shows a
// friendly "demo pending" placeholder instead of the embedded snack.
//
// The embedded demo uses the official embed script (https://snack.expo.dev/embed.js)
// surfaced through `window.ExpoSnack.append()` in SnackEmbed.tsx.
export const SNACK_ID = '@danielking/spirograph-react-native-demo';
